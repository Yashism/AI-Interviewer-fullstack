{
    "builds": [
      {
        "src": "package.json",
        "use": "@vercel/next",
        "config": {
          "skipBuild": true
        }
      }
    ],
    "buildCommand": "npm run build",
    "installCommand": "npm install"
  }